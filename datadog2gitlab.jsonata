(
  {
    'DATADOG_TRANSITION': data.alert.transition,
    'DATADOG_TAGS': data.tags,
    'SLACK_CHANNEL': via.Slack.channel,
    'SLACK_THREAD': via.Slack.ts,
    'DATE_FROM': $fromMillis(data.date - 1000 * 60 * 10),
    'DATE_TO': $fromMillis(data.date)
  }
)