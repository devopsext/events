{{- define "render"}}{{$url := getVar "URL"}}{{printf $url (index . 0) (index . 1)}}{{"\n"}}{{end}}
{{- define "rules"}}
  {{- if .type | regexMatch ".*"}}{{$a := getEnv "EVENTS_TELEGRAM_BOT_PLATFORM" | split "="}}{{template "render" $a}}{{end}}
{{- end}}
{{- define "telegram-selector"}}{{template "rules" .}}{{end}}
